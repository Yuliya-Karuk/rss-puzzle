export enum ButtonCheckStates {
  check = 'Check',
  continue = 'Continue',
}

export enum ButtonSolutionStates {
  solution = 'Solution',
  results = 'Results',
}

export enum DragState {
  toSource = 'toSource',
  toResults = 'toResults',
}
